{
  "compilerOptions": {
    "target": "es5",
    "lib": ["es5", "dom"],
    "types": ["cypress", "node", "cypress-real-events"]  // Dodaj cypress-real-events tutaj
  },
  "include": [
    "**/*.ts",
    "cypress/support/**/*.js",  // Poprawienie include dla całej struktury
    "cypress/integration/**/*.js"  // Dodanie obsługi testów
  ]
}